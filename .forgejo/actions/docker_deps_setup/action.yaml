name: Setup dependencies

runs:
  using: "composite"
  steps:
    - name: "Install deps"
      run: |
        apt install -y sudo curl udev coreutils xz-utils tar
    - name: "Install nix"
      uses: https://github.com/cachix/install-nix-action@v31
